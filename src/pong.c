#include <stdio.h>

int main () {
    prin()
}


int prin() {
    x = 80;
    y = 25;

    for(int y = 0; y < 25; y++)
        for(int x = 0; x < 80; x++) {
            if(!x || x == 24) printf("|");
            else if(y == 0 ) printf("_")
        }
}

